---
import { getCollection } from "astro:content";
import WatergangsectieTypePage from "../../../../../../components/WatergangsectieTypePage.astro";

const entries = await getCollection("datastandaard-objectenhandboek-objects");
const expectedSlug = "/datastandaard/objectenhandboek/watersysteem/watergangsectie/vrij-meanderend";
const entry = entries.find(
  (item) =>
    item.data.slug === expectedSlug ||
    item.data.slug?.replace(/\/$/, "") === expectedSlug ||
    item.data.title === "Vrij meanderende watergang"
);

if (!entry) {
  throw new Error("Objectenhandboek Vrij meanderende watergang ontbreekt in de nieuwe omgeving.");
}
---

<WatergangsectieTypePage entry={entry} sectietype="vrij-meanderend" />
