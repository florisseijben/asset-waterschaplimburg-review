---
import CardLinkIndicator from "./CardLinkIndicator.astro";
import CardTitleIcon from "./CardTitleIcon.astro";
import { withBase } from "../lib/paths";

interface Props {
  title: string;
  text?: string;
  href?: string;
  eyebrow?: string;
  iconTitle?: string;
}

const { title, text, href, eyebrow, iconTitle } = Astro.props;
const Tag = href ? "a" : "article";
---

<Tag class="info-card" href={withBase(href)}>
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <div class="info-card__header">
    <div class="info-card__title-wrap">
      {href && <CardTitleIcon title={iconTitle || title} />}
      <h2 class="info-card__title">{title}</h2>
    </div>
    {href && <CardLinkIndicator />}
  </div>
  {text && <p>{text}</p>}
  <slot />
</Tag>
