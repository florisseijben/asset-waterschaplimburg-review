---
interface Props {
  title: string;
  text: string;
  href?: string;
}

const { title, text, href } = Astro.props;
const Tag = href ? "a" : "article";
---

<Tag class="info-card" href={href}>
  <h2>{title}</h2>
  <p>{text}</p>
  {href && <span class="info-card__link">Open</span>}
</Tag>
